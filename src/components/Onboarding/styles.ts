import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  carouselContainer: {
    marginTop: 40,
  },

  card: {
    width: 260,
    marginRight: 20,
    alignItems: "center",
  },

  image: {
    width: 220,
    height: 220,
    borderRadius: 24,
    backgroundColor: "#ffffff",
  },

  title: {
    marginTop: 14,
    fontSize: 16,
    color: "#fff",
    fontWeight: "600",
    textAlign: "center",
  },
});
